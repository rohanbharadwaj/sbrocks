#include<stdio.h>
#include<stdlib.h>
int main(int argc, char* argv[], char* envp[]) 
{
    //char *buf;
/*     char * cwd; */
	printf("In pwd \n");
    /*buf = malloc(1024);
    if((getcwd(buf, 1024)) != NULL)
            printf("%s\n", buf);
    else
            printf("getcwd() error : ");*/
    return 0;
}